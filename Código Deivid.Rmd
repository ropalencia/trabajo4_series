---
title: "Código Deivid"
author: "Deivid Zhang Figueroa"
date: "2022-11-10"
output: beamer_presentation
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r, echo=FALSE}
library(reticulate)
```

```{python}
import matplotlib.pyplot as plt
import numpy as np
import seaborn as sns
```

## Brindar un marco teórico sobre la serie seleccionada. ¿De qué se trata?, ¿Por qué es importante?, etc.

This is an R Markdown presentation. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document.

## Proceso de lectura y limpieza (si es necesario) de la base de datos hasta crear la serie o series a ser analizadas.

- Bullet 1
- Bullet 2
- Bullet 3

## Justificación del modelo a utilizar y alternativas.

```{r cars, echo = TRUE}
summary(cars)
```

## Presentación de los resultados del ajuste, diagnóstico y medidas remediales para obtener un mejor ajuste (si es necesario).

```{r pressure}
plot(pressure)
```

## Realización y validación de las predicciones.
```{python}
x = np.arange(1,31)
y = np.arange(1,31) + np.random.randn(30)

sns.regplot(x=x, y=y)
plt.show()
```

## Conclusiones y recomendaciones.
```{r}

```

